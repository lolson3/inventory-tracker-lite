# Copy this file to .env for the platform launchers to load it automatically.
# Variables already set in the shell or service take precedence.
HOST=127.0.0.1
PORT=5174
DATA_DIR=/var/lib/inventory-tracker
# Optional: set this when using an HTTPS reverse proxy.
PUBLIC_ORIGIN=https://inventory.example.internal
